"""Single source of truth for the package version.

Keep this in sync with the ``version`` field in ``pyproject.toml`` (or wire up
``hatch-vcs`` later to derive both from git tags).
"""

__version__ = "0.1.1"
